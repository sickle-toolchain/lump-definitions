[package]
name = "lump-definitions"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = ["zerocopy/std"]

[dependencies]
derive_more = { version = "2.1.1", features = ["try_from"] }
zerocopy = "0.8.16"
zerocopy-derive = "0.8.16"
